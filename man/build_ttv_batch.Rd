% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase5_batch.R
\name{build_ttv_batch}
\alias{build_ttv_batch}
\title{Batch-build TTV datasets to disk}
\usage{
build_ttv_batch(
  specs,
  splits,
  time_spec = NULL,
  events = NULL,
  observations = NULL,
  followup = NULL,
  out_dir,
  format = c("qs", "csv", "parquet", "rds"),
  overwrite = FALSE,
  seed = NULL,
  strict = TRUE,
  compress = TRUE,
  manifest_name = "ttv_manifest",
  chunk = list(method = "none", shuffle = TRUE)
)
}
\arguments{
\item{specs}{List of spec objects created by spec_state() and/or spec_event().
\code{spec_decision()} objects are also supported.}

\item{splits}{Output of prepare_splits().}

\item{time_spec}{Optional fluxCore time_spec object passed through to builders.}

\item{events}{Canonical event stream from prepare_events() (required for event tasks).}

\item{observations}{Canonical observation store from prepare_observations() (required for state tasks).}

\item{followup}{Optional follow-up table.}

\item{out_dir}{Directory to write outputs.}

\item{format}{Output format: "qs", "csv", "parquet", or "rds".}

\item{overwrite}{If FALSE, error when output files already exist.}

\item{seed}{Optional integer seed used for chunking and sampling.}

\item{strict}{If TRUE, stop on first error; if FALSE, continue and record errors in the manifest.}

\item{compress}{If TRUE, compress outputs where supported.}

\item{manifest_name}{Base filename for manifest outputs.}

\item{chunk}{Optional list controlling entity chunking. Supported fields:
method ("none", "n_chunks", "chunk_size", or "pct"),
n_chunks,
chunk_size,
pct,
and shuffle (logical).}
}
\value{
A data.frame manifest with class "flux_manifest".
}
\description{
Builds one dataset per spec (and optional entity chunk), writes datasets and metadata to disk, and returns a manifest of outputs.
}
